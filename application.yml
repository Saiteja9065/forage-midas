general:
  kafka-topic: trader-updates
server:
  port: 33400
spring:
  datasource:
    url: "jdbc:h2:mem:foragemidasdb"
    username: admin123
    password: Test@123
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    h2:
      console:
        enabled: true
  kafka:
    consumer:
      auto-offset-reset: earliest